let swiper = new Swiper(".swiper", {
    effect: "cards",
    grabCursor: true,
    initialSlide: 0,  // Commence avec "Le jeu de la dame"
    // loop: false,
    rotate: true,
    mousewheel: {
        invert: false,
    }
});